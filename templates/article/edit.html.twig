{% extends 'base.html.twig' %}

{% block title %}Modifier l'article - Mon Blog{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="page-header">
                    <h1 class="h2">
                        <i class="bi bi-pencil"></i> Modifier l'article
                    </h1>
                    <p class="text-muted">Apportez vos modifications</p>
                </div>

                <div class="card">
                    <div class="card-body p-4">
                        {{ include('article/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
